{
  "description": "Run with debug_force_external=true.",
  "on_init": "SET debug_force_external=true;",
  "skip_compiled": "true",
  "skip_tests": [
    {
      "reason": "These files assert in-memory GroupJoin plans; external execution has dedicated slow tests.",
      "paths": [
        "test/sql/aggregate/group/test_factorized_group_join.test",
        "test/sql/aggregate/group/test_hash_group_join.test"
      ]
    },
    {
      "reason": "Explicit memory limit is exceeded with force external",
      "paths": [
        "test/sql/index/art/storage/test_art_mem_limit.test"
      ]
    }
  ]
}
